{
  "name": "movie-app",
  "private": true,
  "scripts": {
    "dev": "concurrently -n API,WEB -c blue,magenta \"npm run start:dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "build": "npm run build --prefix backend && npm run build --prefix frontend",
    "start": "concurrently -n API,WEB -c blue,magenta \"npm run start:prod --prefix backend\" \"npm run start --prefix frontend\"",
    "seed": "npm run prisma:seed --prefix backend"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
